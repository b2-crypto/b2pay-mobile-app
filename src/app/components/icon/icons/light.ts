import { logo, back, bigLogo, config, expandLess, stepSelected, stepNoSelected } from './light/index.ts';

export default {
  logo,
  back,
  config,
  expandLess,
  bigLogo,
  stepNoSelected,
  stepSelected,
};
